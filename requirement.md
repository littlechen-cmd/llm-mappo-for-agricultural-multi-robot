# 大规模动态仓储 LLM-MAPPO 实施与验收计划书

> 依据：`docs/大规模动态仓储LLM-MAPPO总体方案.md`（v2.0）<br>
> 当前基线：`42b7326 tiny-complete`<br>
> 计划版本：v1.1<br>
> 更新日期：2026-07-30<br>
> 目标架构：LLM 任务分配 -> A* 路径规划 -> MAPPO 局部执行

---

## 1. 计划目的

本计划用于把总体方案转换为可执行、可测试、可逐项验收的研发任务。所有阶段均以明确的交付物和 Go/No-Go 门槛结束；前一阶段未通过时，不进入后一阶段，不使用高层调度或 LLM 掩盖 MAPPO 局部执行问题。

### 1.1 用户明确要求

- 全程做好 Git 版本管理，提交前核对工作树，只提交当前任务相关文件；
- 实施中一旦存在尚未消除的不确定问题，暂停受影响任务并向项目负责人提问；
- 项目最终交付必须包含可视化脚本，用于展示地图、AGV、任务、路径、预约冲突和执行结果；
- 地图基本布局优先参考 vendored `robotic-warehouse/` 环境和 `docs/仿真环境参考.png`，不得脱离现有仓储结构随意设计。

计划执行期间允许并鼓励就以下事项随时向项目负责人提问：

- 需求存在多种合理解释，且不同解释会改变接口或验收结果；
- 方案书与现有 RWARE/MAPPO 行为不一致；
- 需要在性能、复杂度、工期或研究贡献之间作取舍；
- 阶段验收失败，需要决定修复、降级或调整实验范围；
- 需要真实 LLM API、模型、预算、密钥或网络环境；
- 发现现有未提交代码与本计划冲突，且无法在不覆盖用户工作的前提下继续。

默认处理原则：先通过代码、测试、配置和文档进行只读核实；若核实后仍存在不确定性，暂停受影响的实现或修改并提问确认。未受该问题影响的独立只读检查可以继续，但不得以自行假设代替用户确认。

---

## 2. 项目目标与范围

### 2.1 最终目标

构建一个支持动态任务批次和用户优先级调整的多 AGV 仓储系统：

- 地图：`80 x 120`；
- AGV：`10` 台；
- 拣选站：`6` 个；
- 充电站：`10` 个；
- 任务：运行期间随机批次到达，默认 FIFO；
- 优先级：用户显式调整可覆盖默认顺序，但不能突破安全硬约束；
- 调度：LLM 仅在事件触发时进行低频任务分配；
- 路径：A* 负责全局路径和简单多 AGV 预约；
- 执行：MAPPO 负责 waypoint 跟踪、局部避障、装卸和充电；
- 降级：LLM 不可用时自动回退到 FIFO + Nearest-First，任务流继续运行。

### 2.2 银牌交付目标

若最终规模不能在计划周期内稳定通过验收，则交付：

- 地图：`仿真环境中的large`；
- AGV：`6` 台；
- 完整三层架构；
- 动态任务、用户优先级、安全校验、LLM 故障回退和完整指标报告均可用。

银牌目标是明确的降级交付，不等同于最终目标通过。

### 2.3 本期不作为前置条件的内容

- 高频 LLM 逐 step 控制；
- LLM 直接输出环境动作；
- 高频 LLM 教师蒸馏；
- 复杂知识库或检索增强异常处理；
- 超出简单预约表需求的完整 MAPF/CBS 系统；
- 未经单独验收的 Z-RHMCF 作为主调度器；
- 为追求规模而跳过 MAPPO oracle Go/No-Go。

---

## 3. 当前基线与已知问题

### 3.1 已提交基线

当前 `HEAD` 为 `133b4e7 small-2ag-final`。已提交主链路为：

```text
RulePlanner / RuleBasedPriorPolicy
                |
                v
WarehouseStateAdapter
                |
                v
MAPPOExecutor（共享 Actor + 集中式 CNN Critic）
                |
                v
vendored robotic-warehouse 环境
```

该基线已经具备：

- 高层规划快照、任务分配和规划决策接口；
- RWARE 到 MAPPO 的状态编码；
- 局部栅格、全局地图、动作掩码和规则先验；
- MAPPO Actor/Critic、PPO、GAE、checkpoint 和 TensorBoard；
- small/2-AGV 训练、评估和可视化入口。

### 3.2 基线测试状态

2026-07-29 定向测试结果为 `15 passed, 3 failed`。已知失败：

- `AgentSnapshot` 缺少路径能耗预算字段；
- `LegalPathRewardShaper` 缺少安全充电相关参数和奖励逻辑；
- `RuleBasedPriorPolicy` 未处理两个 AGV 请求同一前进格的让行冲突。

因此，`small-2ag-final` 当前是回退起点，但尚不能称为测试全绿或验收通过的稳定基线。

### 3.3 未提交候选代码

工作树中存在未提交的动态 benchmark、FIFO/Nearest/Auction/Z-RHMCF 调度、承诺账本、时空预约路径、交通适配和评估脚本。这些代码属于用户现有工作，执行本计划时：

- 不擅自删除、覆盖或回退；
- 在完成基线隔离和接口审查前，不默认纳入主线；
- 与 v2.0 架构一致的部分可经测试后复用；
- Z-RHMCF 仅保留作历史对照或消融，不作为目标架构主调度器；
- 任何复用决定均记录来源、修改范围和验证结果。

---

## 4. 总体技术架构

### 4.1 分层职责

| 层             |                  调用频率 | 输入                                             | 输出                       | 不承担的职责                            |
| -------------- | ------------------------: | ------------------------------------------------ | -------------------------- | --------------------------------------- |
| LLM 任务分配   | 事件驱动，间隔不小于 5 秒 | 动态批次、AGV 摘要、站点状态、用户约束、异常摘要 | 经验证的任务分配           | 不输出逐 step 动作，不直接写环境        |
| A* 路径规划    |    新任务或重规划事件触发 | 任务分配、地图、静态障碍、预约状态               | waypoint、ETA、预约结果    | 不改变任务优先级，不替代 MAPPO 局部策略 |
| MAPPO 局部执行 |             每个环境 step | 局部观测、当前 waypoint、动作掩码、任务语义特征  | 离散环境动作、执行异常事件 | 不进行全局任务分配或长距离搜索          |

### 4.2 必须冻结的数据协议

在接入真实 LLM 前，至少冻结以下结构并携带 `schema_version`：

- `TaskBatch`：批次 ID、到达 step、任务列表、默认/用户优先级；
- `WarehouseSnapshot`：AGV、任务、站点、地图摘要和事件；
- `TaskAssignment`：AGV-任务绑定、目标、优先级、锁定状态；
- `DispatchPlan`：分配、延后任务、来源、有效期、回退原因；
- `RoutePlan`：waypoint、ETA、预约、重规划原因；
- `ExecutionEvent`：`STALLED`、`BLOCKED`、`REPLAN_REQUIRED`、故障和任务状态变化；
- `LLMAuditRecord`：调用原因、延迟、token、解析结果、校验结果、缓存和回退信息。

### 4.3 硬约束

- 同一任务不能同时分配给多个 AGV；
- 同一 AGV 在锁定期间不能被重分配；
- 搬起货架、进入不可中断阶段或处于窄通道锁定时，任务所有权保持不变；
- 分配前必须检查电量是否足够覆盖取货、送达和安全余量；
- 路径不可达、站点容量不足或 AGV 类型不匹配时拒绝分配；
- LLM 解析失败、超时或输出非法时使用确定性回退；
- 每个环境 step 的动作循环中不得调用 LLM；
- 所有实验条件必须使用相同地图、任务流、用户事件和随机种子。

---

## 5. 里程碑总览

| 里程碑 | 结果                 | 前置条件      | Go/No-Go 摘要                               |
| ------ | -------------------- | ------------- | ------------------------------------------- |
| M0     | small-2ag 基线恢复   | 当前仓库      | 基线测试全绿，评估可复现                    |
| M1     | 环境与协议冻结       | M0            | 25x25/3-AGV 环境稳定，基础 A* 单测通过      |
| M2     | MAPPO oracle 验证    | M1            | 完成率、碰撞、死锁、收敛、10 seeds 全部达标 |
| M3     | A* + MAPPO 集成      | M2            | 相对 oracle 完成率下降不超过 5%             |
| M4     | 50x50/6-AGV LLM 分配 | M3            | LLM 相对 FIFO 至少两项核心指标显著改善      |
| M5     | 80x120/10-AGV 验收   | M4            | 完整指标、故障注入、长期运行均通过          |
| M6     | 可选增强             | M5 或独立批准 | 不影响主交付稳定性                          |

---

## 6. 详细任务清单

状态约定：`[ ]` 未开始，`[-]` 进行中，`[x]` 已完成，`[!]` 阻塞或未通过。

### Phase 0：基线隔离与恢复

目标：得到一个测试全绿、训练和评估可重复的 small/2-AGV 基线，作为后续所有变更的参照。

#### 0.1 工作树与基线保护

- [x] P0-01 记录当前 `HEAD`、分支、完整 `git status --short` 和未提交文件清单。
- [x] P0-02 与项目负责人确认未提交候选代码的处理方式：保留原位、迁移到独立分支或仅做只读参考。
- [x] P0-03 建立“基线文件”和“候选文件”边界，不将动态调度候选误计入 small-2ag 验收。
- [x] P0-04 确认生成目录、checkpoint、TensorBoard 日志继续保持不提交。

执行记录（2026-07-29）：`main` 指向 `133b4e70d408de4d93b3d93460094505d7270a1e`（`small-2ag-final`）；工作树含 4 个已删除的已跟踪文档和 41 个未跟踪项。项目负责人确认候选代码保留原位，仅作只读候选实现；动态 benchmark、调度、交通、承诺、对应测试/训练/配置不计入 small-2ag 基线。`.gitignore` 已确认忽略 `artifacts/`、`runs/`、checkpoint 和 `.env`。未移动、删除、暂存或提交任何现有文件。

#### 0.2 修复基线契约

- [x] P0-05 补齐或重新确认 `AgentSnapshot.collection_energy_costs` 的语义、单位和来源。
- [x] P0-06 实现并测试安全充电 streak 奖励，确保奖励不会重复刷取。
- [x] P0-07 为规则先验增加同一目标格竞争的确定性让行规则。
- [x] P0-08 运行 MAPPO executor、课程奖励、checkpoint、TensorBoard 和 baseline closure 测试。
- [x] P0-09 运行完整 `pytest`，区分基线失败与未提交候选测试失败。

#### 0.3 可复现评估

- [x] P0-10 固定 small-2ag 评估配置、checkpoint、seed 集和指标口径。
- [x] P0-11 使用独立 selection seeds 与 held-out acceptance seeds。
- [x] P0-12 输出每 episode 诊断、聚合均值、标准差和失败原因。
- [x] P0-13 记录 small-2ag baseline closure 最终是否通过；不得只报告最佳 seed。

#### Phase 0 验收

- [x] G0-01 已提交基线相关测试全部通过。
- [x] G0-02 完整测试结果中的候选代码失败已单独归类。
- [x] G0-03 相同配置和 seed 重复评估结果一致。
- [x] G0-04 形成基线报告，包含 checkpoint、配置、commit、环境和指标。
- [!] A0-01 项目负责人审阅 Phase 0 基线报告，并决定进入 Phase 1、重新开启 small-2ag 优化或调整项目范围。

未通过处理：停止扩展，先修复基线；若测试期望与项目真实需求冲突，向项目负责人提问后修改契约或测试，不静默删除测试。

处理记录（2026-07-29）：项目负责人确认采用 `0.0-1.0` 归一化电量。RWARE、训练/评估入口和已跟踪配置已统一为 `initial_battery=1.0`、`max_battery=1.0`、`safe_charge=0.5`；动作耗电按确认表实现：空载移动/转向 `0.001`、装卸和载货移动/转向 `0.002`、等待/非法/锁定 `0.0002`、成功充电 `0.0`。`AgentSnapshot` 现提供保守的取货-拣选-充电预算，规则规划器在预算不足时优先充电；安全充电 streak 和同目标格确定性让行均已回归测试。

验收记录（2026-07-29）：已跟踪基线测试和 vendored RWARE 测试共 `32 passed`；额外 baseline closure/迁移测试 `4 passed`。完整 `pytest` 因 5 个未跟踪的动态调度/交通候选测试在收集期要求尚未冻结的 v2 接口而停止，已单独归类。当前固定评估使用 `configs/mappo_small_2ag_refinement.yaml`、`artifacts/mappo_small_2ag_refined.pt`、selection seeds `100042-100071` 和 held-out seeds `200042-200141`。100-seed held-out 结果为 full success `0.00%`、平均完成 `0.00/6`、死亡 `0`，未通过 closure；10-seed 重复评估与 held-out 报告前 10 个 episode 诊断逐字段一致。详见 `docs/phase0_small_2ag_baseline_report.md`。

---

### 当前恢复路径：tiny-2AG MAPPO 基线

> 变更记录（2026-07-29）：small-2AG 历史 Actor 在已尝试的电量尺度、观测和执行契约下均无法复现历史 full success，项目负责人已授权回退。工作树已重置至 `42b7326 tiny-complete`；未跟踪的动态调度、交通和候选实验文件保留原位，仅作只读候选实现。本恢复路径的完成是重新启动后续大规模工作前的强制前置条件。

目标：在与 `tiny-complete` 匹配的总电量 `10.0` 环境中，训练并验证不依赖规则动作混合的 tiny/2-AGV 共享 Actor；训练、评估、可视化和指标口径均可复现。

#### T0：环境和训练契约

- [x] T0-01 核对 `tiny-complete` 提交、未跟踪候选文件边界和现有 tiny checkpoint 的加载兼容性。
- [x] T0-02 将环境电量恢复为总电量 `10.0`，并按确认的动作表实现耗电：空载移动/转向 `0.001`，成功空载装卸 `0.002`，载货成功移动/转向/装卸 `0.002`，NOOP、非法/失败和拣选锁定 `0.0002`，成功充电不扣电。
- [x] T0-03 为初始/最大电量、观测归一化、充电上限、逐动作耗电、失败动作和锁定状态增加确定性环境测试。
- [x] T0-04 运行已跟踪的基线测试，候选测试另行报告且不作为 tiny 基线结论。

#### T1：1-AGV 初始化验证

- [x] T1-01 Actor-only 评估现有 `artifacts/mappo_tiny_1ag.pt`，记录固定 seed 集上的完成率、full success、死亡和完成步数。
- [x] T1-02 历史 1-AGV checkpoint 与修复后的电量契约兼容，20 个固定 episode 均完成 `3/3` 任务、full success `100%`、死亡 `0`、平均完成步数 `96`；无需重新训练。
- [x] T1-03 验证 1-AGV Actor 可严格加载到 2-AGV 共享 Actor，且 Critic、优化器在新阶段重新初始化。

#### T2：tiny-2AG 训练和验证

- [x] T2-01 从已验证的 1-AGV Actor 按 `configs/mappo_tiny_2ag.yaml` 训练 tiny/2-AGV；共训练 `1200` episodes，初始化仅加载 `artifacts/mappo_tiny_1ag.pt` 的共享 Actor，Critic 和优化器重新初始化；评估时规则动作混合为零。
- [x] T2-02 在 held-out seeds `200042-200061` 上输出 episode 级诊断及汇总：`20/20` full success、平均完成 `4/4`、死亡 `0`、平均 `75` steps。
- [x] T2-03 用相同 checkpoint、配置和 held-out seeds 重复评估，两个 JSON 的逐 episode 诊断完全一致。
- [x] T2-04 已生成固定 seed `200042` 的 headless 成功轨迹 GIF：`artifacts/mappo_tiny_2ag_recovery_seed200042.gif`。

#### Tiny-2AG 人工验收门

- [x] GT-01 已跟踪基线测试和本轮新增测试通过；电量动作表具有自动化回归覆盖。
- [x] GT-02 2-AGV Actor-only held-out full success、平均完成任务数、死亡和重复性均已报告；规则动作混合为 `0`。
- [x] GT-03 已提供训练配置、初始化 checkpoint、最终 checkpoint、seed 列表和可视化产物。最终 checkpoint 为 `artifacts/mappo_tiny_2ag_recovery.pt`。
- [x] AT-01 项目负责人已接受 tiny-2AG 恢复基线，并授权进入 Phase 1。

### Phase 1：仿真环境与接口冻结

目标：采用现成 RWARE 风格 `medium` 环境构建可复现的动态任务环境，冻结三层协议并验证基础 A*。仅当 medium 在环境、路径或稳定性验收上不可控时才降级为 `small`。

规模要求：支持 `1/3/5` AGV、`2/4` 个拣选站，以及 `1/3/5` 个充电站；布局生成强制 `charger_count == agv_count`。默认验证矩阵为 `(1 AGV, 2 picker, 1 charger)`、`(3 AGV, 2 picker, 3 charger)` 和 `(5 AGV, 4 picker, 5 charger)`。训练和评估 seed 必须影响初始请求货架位置，训练 seed 池与 held-out seed 池不得重叠。

#### 1.1 环境与任务流

- [x] P1-00 对照 vendored RWARE 布局，确定 medium 环境中的可通行区、货架区、左侧服务行 picker/dock、右侧服务区 charger/AGV 出生格和窄通道布局。
- [x] P1-01 扩展 `robotic-warehouse/rware/heterogeneous.py` 的 RWARE 风格布局生成器，复用 vendored 环境而未引入不兼容的新环境。
- [x] P1-02 支持 `1/3/5` AGV 运行模式，并验证默认规模矩阵。
- [x] P1-03 支持最多 `4` 个 picker 和与 AGV 数量一致的 `1/3/5` charger，站点数量可配置。
- [x] P1-04 实现泊松任务控制器，默认到达强度为每 `20` step `1` 个任务；控制器清理 RWARE 自动补货产生的未计划 request，使任务来源可审计。
- [x] P1-05 `randomize_initial_requests=True` 时，`reset(seed=...)` 用 Gym RNG 随机选择请求货架；训练 seed 池 `1000-1099` 与 held-out seed 池 `2000-2019` 分离，100-episode 稳定性验证使用 `2000-2099`。
- [x] P1-06 已实现任务生命周期 `PENDING -> ASSIGNED/LOCKED -> PICKED -> DELIVERING -> COMPLETED/FAILED`，包含 AGV owner 锁定与非法迁移拒绝。

#### 1.2 数据协议

- [x] P1-07 定义并测试 `TaskBatch`、`WarehouseSnapshot`、`DispatchAssignment` 和 `DispatchPlan`。
- [x] P1-08 定义并测试 `RoutePlan`、时空/边预约记录和 ETA 字段。
- [x] P1-09 定义 `ExecutionEventType`、`ExecutionEvent`，并在 `PICKING_STARTED` 事件中提供 `shelf_id`。
- [x] P1-10 为 v1 外部输入增加 schema 校验、未知字段拒绝和清晰错误信息。
- [x] P1-11 已为 `TaskBatch`、`RoutePlan`、`DispatchPlan` 增加 v1 dict/JSON round-trip 与版本兼容测试。

#### 1.3 基础 A*

- [x] P1-12 新增 `AStarRoutePlanner`，使用 4 方向网格与曼哈顿启发函数。
- [x] P1-13 路径规划复用环境的合法阻塞规则，覆盖 picker、死亡 AGV、可选 live AGV 和载货时其他货架。
- [x] P1-14 输出完整 waypoint、路径长度、ETA 和不可达原因。
- [x] P1-15 20 个 medium 固定目标合法路径测试通过。
- [x] P1-16 已测试起点等于终点、越界、动态占用目标和不可达边界。

#### 1.4 手动和稳定性验证

- [x] P1-17 已完成单 AGV 真实环境动作回放：step `10` 装货、step `32` 开始拣选、step `34` 完成拣选、step `56` 充电；产物为 `artifacts/phase1_manual_1ag.gif` 和对应 JSON。
- [x] P1-18 连续运行 `100` 个 medium 动态 held-out episodes：`0` crash、`0` death、平均完成 `11.47` 任务；100 个 seed 对应 100 条不同的任务释放序列。
- [x] P1-19 已输出环境和稳定性记录：`configs/phase1_medium_dynamic.yaml`、`artifacts/phase1_medium_stability.json`，以及 seed `2000` 的动态环境 GIF `artifacts/phase1_medium_seed2000.gif`（277 steps、完成 12 个任务、14 个动态批次）。

#### Phase 1 验收

- [x] G1-01 100 episodes 无 crash、无 death，未发现状态污染。
- [x] G1-02 基础 A* 单元测试全部通过。
- [x] G1-03 数据协议测试全部通过并冻结 v1 schema。
- [x] G1-04 单 AGV 手动任务生命周期已有完整可视化和事件记录。

#### Phase 1 人工验收门

- [!] A1-01 项目负责人审阅 Phase 1 环境、动态任务、协议、路径、稳定性报告与两份可视化产物，并决定进入 Phase 2 或调整 medium 范围。在人工验收前不得启动 Phase 2。

需要确认的问题：现有 RWARE 使用朝向相关动作，而方案书描述绝对方向动作。默认建议保留 RWARE 的 `NOOP/FORWARD/LEFT/RIGHT/TOGGLE_LOAD/CHARGE` 语义，在状态和 waypoint 层适配；若要改成绝对方向动作，必须先确认，因为这会改变 Actor 输出头、已有 checkpoint 和所有动作测试。

---

### Phase 2：MAPPO 局部执行验证（核心风险关卡）

目标：在 oracle 任务分配和 oracle 路径条件下，独立证明 MAPPO 能稳定执行 waypoint 跟踪、局部避障、装卸和充电。

#### 2.1 Oracle 执行环境

- [ ] P2-01 实现确定性 FIFO oracle 调度器，仅负责给出唯一合法任务。
- [ ] P2-02 使用忽略其他活动 AGV 的静态 A* 作为 oracle 路径。
- [ ] P2-03 每个 Actor 只接收当前 waypoint，不暴露完整未来路径作弊信息。
- [ ] P2-04 将调度失败、路径失败和 Actor 执行失败分开统计。

#### 2.2 观测和动作

- [ ] P2-05 冻结局部网格尺寸，默认验证方案书的 `9 x 9`，并记录与当前 `local_radius=3` 的差异。
- [ ] P2-06 加入自身电量、载货、朝向/速度和当前 waypoint 相对位置。
- [ ] P2-07 加入最近 3 个 AGV 的相对状态并定义缺失邻居填充值。
- [ ] P2-08 加入任务紧急度和区域拥堵两个语义特征。
- [ ] P2-09 保证动作掩码覆盖越界、碰撞、非法装卸和非法充电。

#### 2.3 网络与训练

- [ ] P2-10 先完成 1 AGV 训练，证明基础 waypoint 跟踪收敛。
- [ ] P2-11 进行 1 AGV 的多 seed 复现，不以单次最佳曲线作为通过依据。
- [ ] P2-12 扩展到 3 AGV 参数共享 Actor 和集中式 Critic。
- [ ] P2-13 对比现有 CNN Critic 与方案书注意力池化 Critic，按验证结果选择，不先假定重写必要。
- [ ] P2-14 检查 PPO ratio、KL、entropy、value loss、gradient norm 和 explained variance。
- [ ] P2-15 实现 checkpoint 完整恢复和 Actor-only transfer 测试。

#### 2.4 奖励与异常

- [ ] P2-16 实现 waypoint 到达、任务完成、时间、碰撞、低电量和连续等待奖励。
- [ ] P2-17 检查每个 dense reward 是否可能被原地循环、重复装卸或充电刷取。
- [ ] P2-18 定义 `STALLED`：连续 30 步未缩短到 waypoint 的有效距离。
- [ ] P2-19 定义 `BLOCKED`：所有可执行移动均被合法占用或预约拒绝。
- [ ] P2-20 定义 `REPLAN_REQUIRED`：路径失效、障碍变化或 waypoint 不可达。

#### 2.5 评估

- [ ] P2-21 使用至少 10 个 held-out seeds，每个 seed 使用相同 episode 数和上限。
- [ ] P2-22 报告任务完成率、完整成功率、碰撞、死锁、死亡、等待和训练步数。
- [ ] P2-23 输出失败 episode 的轨迹、动作、waypoint 和事件诊断。
- [ ] P2-24 确认评估时关闭规则动作混合，只评估 Actor 本身。

#### Phase 2 验收

- [ ] G2-01 任务完成率 `>= 95%`。
- [ ] G2-02 每 episode 碰撞次数 `<= 2`。
- [ ] G2-03 死锁率 `<= 5% episodes`。
- [ ] G2-04 `<= 5000 episodes` 内训练指标稳定。
- [ ] G2-05 10 seeds 成功率方差 `<= 10%`。
- [ ] G2-06 Actor-only 评估通过，不依赖规则动作混合掩盖失败。

未通过处理：按观测不足、奖励漏洞、动作约束、训练不稳定和环境不可解五类归因；提交诊断后向项目负责人确认下一轮实验范围，不进入 Phase 3。

---

### Phase 3：A* 路径层与 MAPPO 集成

目标：使用真实路径层替代 oracle 路径，并增加简单、可解释的多 AGV 协调。

#### 3.1 路径与预约

- [ ] P3-01 审查现有 `ReservationAwareRoutePlanner`，确认其能否作为 v2 A* 基础复用。
- [ ] P3-02 支持顶点冲突和对向边冲突检测。
- [ ] P3-03 支持窄通道进入前的时间段预约。
- [ ] P3-04 支持交叉口确定性优先级，平局规则必须可复现。
- [ ] P3-05 支持等待、绕行和有限 horizon 的重规划。
- [ ] P3-06 预约失败必须原子回滚，不能留下部分占用。

#### 3.2 任务承诺与执行同步

- [ ] P3-07 冻结任务承诺状态机及合法状态转移。
- [ ] P3-08 在取货或不可中断执行后锁定任务所有权。
- [ ] P3-09 在重规划时保留任务所有权，只替换路线。
- [ ] P3-10 将 RWARE 的装载、拣选、完成、死亡事件同步到承诺账本。
- [ ] P3-11 为非法状态转移、部分预约、抢占和回滚增加测试。

#### 3.3 MAPPO 集成

- [ ] P3-12 将当前 waypoint 和路线状态接入 `WarehouseStateAdapter`。
- [ ] P3-13 将 MAPPO 异常事件路由到路径层，不直接触发 LLM。
- [ ] P3-14 验证 waypoint 切换不会破坏 rollout、奖励基线或 plan ID。
- [ ] P3-15 分别记录计划路径长度、实际路径长度、ETA 偏差和重规划次数。

#### Phase 3 验收

- [ ] G3-01 顶点、边、窄通道和交叉口冲突测试全部通过。
- [ ] G3-02 预约、承诺和环境事件保持一致，无部分提交。
- [ ] G3-03 集成后任务完成率相对 Phase 2 下降不超过 `5%`。
- [ ] G3-04 MAPPO step 循环不含 LLM 调用。

---

### Phase 4：LLM 任务分配层接入

目标：在 `50 x 50 / 6 AGV` 场景实现低频、可校验、可回退的 LLM 任务分配。

#### 4.1 调度基线

- [ ] P4-01 保留 FIFO 作为主验收基线。
- [ ] P4-02 实现或审查 Nearest-First，确保与 FIFO 的优先级定义独立可比。
- [ ] P4-03 将 Auction/Z-RHMCF 标记为可选历史对照，不参与目标架构控制链路。
- [ ] P4-04 所有调度器使用完全相同的动态任务流、地图和随机种子。

#### 4.2 LLM Provider 与调用控制

- [ ] P4-05 定义可替换 `LLMProvider` 接口，默认候选为 DeepSeek-V3。
- [ ] P4-06 支持 mock、真实 API 和故障注入 provider。
- [ ] P4-07 实现不少于 5 秒的调用间隔和每 episode 调用上限。
- [ ] P4-08 实现状态摘要内容哈希缓存，重复状态不重复调用。
- [ ] P4-09 设置 5 秒超时，并区分超时、网络、解析和安全拒绝。
- [ ] P4-10 API 密钥只通过环境变量注入，不写入配置、日志或仓库。

#### 4.3 Prompt 与结构化输出

- [ ] P4-11 Prompt 只包含任务分配需要的结构化摘要，不发送逐 step 原始轨迹。
- [ ] P4-12 输出采用固定 JSON schema，包含 assignments、deferred 和简短理由。
- [ ] P4-13 限制任务/AGV ID 必须来自输入，不允许模型发明对象。
- [ ] P4-14 对锁定 AGV、重复任务、未知任务和非法空值增加解析测试。

#### 4.4 安全校验与回退

- [ ] P4-15 校验电量可达性和安全余量。
- [ ] P4-16 校验 AGV/任务类型匹配。
- [ ] P4-17 校验任务锁定和不可抢占状态。
- [ ] P4-18 校验拣选站、充电站容量和路线可达性。
- [ ] P4-19 非法分配只拒绝受影响项；若整体不可用则回退 FIFO + Nearest-First。
- [ ] P4-20 LLM 完全不可用时任务流必须持续运行。

#### 4.5 审计和实验

- [ ] P4-21 记录调用触发原因、缓存命中、P50/P95 延迟、token 和估算成本。
- [ ] P4-22 记录解析失败、安全拒绝和回退比例。
- [ ] P4-23 完成 FIFO、Nearest-First、LLM 和 LLM 故障注入消融。
- [ ] P4-24 每个条件至少使用 10 个相同 held-out seeds。
- [ ] P4-25 对核心指标进行预注册统计检验，显著性阈值 `p < 0.05`。

#### Phase 4 验收

- [ ] G4-01 LLM 调用成功率 `>= 95%`。
- [ ] G4-02 任务饥饿率 `<= 3%`。
- [ ] G4-03 下列指标至少两项显著优于 FIFO：吞吐 `>= +10%`、P95 等待 `>= -15%`、高优先级响应 `>= -30%`。
- [ ] G4-04 LLM 故障后自动回退，任务流不中断且退化可量化。
- [ ] G4-05 代码级检查确认 MAPPO 逐 step 循环没有 LLM 调用。

实施前提问点：确认实际 LLM provider、模型版本、API 预算、允许的每 episode 调用上限，以及验收环境是否具备稳定网络。若未确认，先使用 mock provider 完成协议、安全和回退测试。

---

### Phase 5：80x120/10-AGV 目标规模扩展

目标：在最终规模完成性能、稳定性和完整验收。

#### 5.1 环境扩展

- [ ] P5-01 构建 80x120 地图、10 AGV、6 拣选站、10 充电站配置。
- [ ] P5-02 定义目标任务负载、批次大小、到达率和长期运行时长。
- [ ] P5-03 验证地图连通性、站点容量和任务可解性。

#### 5.2 模型和路径性能

- [ ] P5-04 评估现有 Critic 在大地图上的显存、速度和感受野。
- [ ] P5-05 仅在证据表明需要时升级为多尺度地图编码。
- [ ] P5-06 对 A* 实施区域分区、路径缓存或搜索边界优化。
- [ ] P5-07 测量单次规划 P50/P95 延迟、重规划延迟和 step 时间。
- [ ] P5-08 保证执行循环满足目标实时性，不因日志或 LLM 阻塞。

#### 5.3 LLM 摘要扩展

- [ ] P5-09 在状态摘要中加入区域、站点负载和拥堵信息。
- [ ] P5-10 控制 prompt 长度随 AGV/任务规模增长的上界。
- [ ] P5-11 验证 10 AGV 下 JSON 完整性、安全拒绝率和回退率。

#### 5.4 最终实验

- [ ] P5-12 使用至少 10 个 held-out seeds 完成 FIFO、Nearest-First、LLM 对照。
- [ ] P5-13 报告吞吐、平均/P95 等待、高优先级响应、饥饿率和站点均衡。
- [ ] P5-14 报告能耗、死亡、碰撞、死锁、阻塞、重规划和预约冲突。
- [ ] P5-15 报告 LLM 调用次数、P50/P95 延迟、token、成本、拒绝和回退。
- [ ] P5-16 完成 LLM 全故障、延迟、非法 JSON 和不安全分配注入。
- [ ] P5-17 完成长时间运行，检查内存增长、预约泄漏和任务状态泄漏。
- [ ] P5-18 提供最终可视化脚本，展示 AGV 位置/朝向、任务优先级、当前 waypoint、规划路径、预约冲突、充电和任务状态。
- [ ] P5-19 可视化脚本支持固定 seed、加载 checkpoint、切换 FIFO/Nearest/LLM 模式，并能导出 GIF 或视频结果。

#### Phase 5 验收

- [ ] G5-01 80x120/10-AGV 环境可稳定长时间运行。
- [ ] G5-02 所有安全硬约束无违规。
- [ ] G5-03 完整指标和 10-seed 统计报告可复现。
- [ ] G5-04 LLM 故障注入下系统保持可用。
- [ ] G5-05 最终演示、配置、checkpoint、报告和运行说明完整。
- [ ] G5-06 可视化脚本能够复现至少一个正常协同场景、一个拥堵/重规划场景和一个 LLM 故障回退场景。

若目标规模未通过：根据瓶颈证据决定继续优化或启用 50x50/6-AGV 银牌交付，必须由项目负责人确认，不能自动将银牌结果声明为最终目标通过。

---

### Phase 6：可选增强

- [ ] P6-01 建立异常事件知识库和相似案例检索。
- [ ] P6-02 实现低频 LLM 异常重规划，且 MAPPO 执行不中断。
- [ ] P6-03 增加知识库、异常重规划和无增强三组消融。
- [ ] P6-04 证明增强不破坏 Phase 5 的安全、回退和实时性。

Phase 6 不阻塞主项目验收，只有在 Phase 5 稳定或项目负责人明确批准后启动。

---

## 7. 测试与验收矩阵

| 测试层级 | 覆盖内容                                              | 最低要求                   |
| -------- | ----------------------------------------------------- | -------------------------- |
| 单元测试 | 数据结构、校验器、奖励、动作掩码、A*、预约、状态机    | 快速、确定性、无网络依赖   |
| 集成测试 | RWARE + 调度 + 路径 + MAPPO 事件闭环                  | 固定 seed，可输出失败轨迹  |
| 回归测试 | small-2ag 基线、checkpoint、训练迁移、TensorBoard     | 每次核心修改后运行         |
| 故障测试 | LLM 超时、非法 JSON、不安全输出、路径不可达、AGV 死亡 | 自动回退，无部分提交       |
| 性能测试 | A* 延迟、step 延迟、吞吐、内存、LLM RTT               | 报告 P50/P95 和测试环境    |
| 统计实验 | FIFO、Nearest、LLM、故障注入                          | 相同 seeds，至少 10 seeds  |
| 最终验收 | 80x120/10-AGV 或经确认的银牌目标                      | 完整报告、演示和可复现配置 |

建议常用命令：

```powershell
conda run -n py310 python -m pytest
conda run -n py310 python -m pytest tests/test_mappo_executor.py
conda run -n py310 python train/train_mappo.py --config <config>
conda run -n py310 python train/evaluate_mappo.py --help
```

若当前会话找不到 `conda`，可使用已确认的解释器：

```powershell
D:\Anaconda3\envs\py310\python.exe -m pytest
```

---

## 8. 指标定义

所有指标必须在代码和报告中给出明确分母、时间单位和空样本处理方式。

| 指标         | 定义要求                                         |
| ------------ | ------------------------------------------------ |
| 任务吞吐     | 完成任务数 / 有效环境 step 或模拟时间            |
| 平均等待     | 从任务到达到任务完成的平均时长                   |
| P95 等待     | 所有已完成任务等待时长的 95 分位                 |
| 高优先级响应 | 用户覆盖后到首次有效服务或完成的时长，需固定口径 |
| 饥饿率       | 等待超过阈值的任务数 / 已到达任务数              |
| 站点负载均衡 | 各拣选站完成任务数的标准差或变异系数             |
| 能耗         | 总电量消耗 / 完成任务数                          |
| 死锁率       | 需要外部干预的 episodes / 总 episodes            |
| 重规划率     | 重规划次数 / episode，并区分触发原因             |
| 预约冲突率   | 冲突预约请求 / 总预约请求                        |
| LLM 成功率   | 成功解析且通过安全校验的调用 / 总调用            |
| LLM 回退率   | 进入确定性回退的调用 / 总调用                    |

---

## 9. 产物与版本管理

每个阶段至少产生：

- 对应源码和配置；
- 单元/集成测试；
- 一份阶段结果摘要；
- 运行命令和环境信息；
- 指标 JSON 或 CSV；
- 失败样本和诊断信息；
- Go/No-Go 结论；
- 尚未解决的问题和下一阶段风险。

版本管理要求：

- 提交前检查 `git status --short`；
- 只暂存当前任务相关文件；
- 不提交 `.env`、API key、checkpoint、`artifacts/`、`runs/` 和长期日志；
- 不覆盖用户现有未提交改动；
- 配置、checkpoint 和结果报告必须记录对应 commit hash；
- 实验配置优先写入 YAML，不在脚本中硬编码。

---

## 10. 执行中的提问与变更机制

### 10.1 必须提问的情形

- 动作空间选择会使已有 checkpoint 失效；
- 数据协议需要破坏性变更；
- 需要删除、迁移或覆盖未提交候选代码；
- Go/No-Go 未通过但希望继续下一阶段；
- 需要更换 LLM provider、模型或统计验收口径；
- 最终目标需要降级为银牌目标；
- 新需求会显著增加范围或改变研究主张。

### 10.2 提问格式

执行过程中问题应尽量包含：

```text
问题：需要决定什么？
背景：为什么现在必须决定？
选项 A：影响、成本和风险。
选项 B：影响、成本和风险。
建议：推荐选项及理由。
阻塞范围：哪些任务可以继续，哪些任务必须等待。
```

### 10.3 遇到不确定问题时的处理

发现不确定问题后，先停止受影响任务，不进行可能需要返工的代码修改、训练或实验。可以继续收集只读证据，并按 10.2 的格式向项目负责人提问；收到确认后再恢复对应任务。与该问题完全独立的任务可以继续，但需要在状态更新中注明边界。

### 10.4 任务状态更新

实施期间直接更新本文件中的复选框：

- 开始任务：`[ ] -> [-]`；
- 完成并验证：`[-] -> [x]`；
- 阻塞或验收失败：`[-] -> [!]`；
- 只有代码完成且相应测试通过后才能标记 `[x]`；
- 每次阶段结束更新 Go/No-Go、测试命令、结果摘要和遗留问题。

---

## 11. 计划启动前待确认项

以下问题不阻塞本计划书生成，但在对应任务启动前需要确认：

- [ ] Q-01 未提交的动态调度/交通候选代码应保留原位，还是后续迁移到独立分支？
- [ ] Q-02 MAPPO 是否保留现有 RWARE 朝向动作语义？默认建议保留。
- [ ] Q-03 方案书的 `9 x 9` 局部网格是否为硬性要求？当前基线默认是 `7 x 7`。
- [ ] Q-04 Phase 2 的“任务完成率”分母按已到达任务、可解任务还是每 episode 固定目标计算？
- [ ] Q-05 Phase 4 是否固定使用 DeepSeek-V3，还是只要求 provider 可替换？
- [ ] Q-06 每 episode 的 LLM 调用预算采用 4、8、12 次还是按场景配置？默认建议可配置、基准值为 8。
- [ ] Q-07 最终验收的长期运行时长、动态任务到达率和批次规模具体取值。

---

## 12. 首轮执行顺序

计划批准后，首轮只执行以下任务：

1. P0-01 至 P0-04：保护和隔离当前工作树；
2. P0-05 至 P0-09：恢复基线契约和测试；
3. P0-10 至 P0-13：完成 small-2ag 可复现评估；
4. 提交 Phase 0 报告和 G0-01 至 G0-04 结论；
5. Phase 0 通过后，再开始 Phase 1 环境和协议工作。

任何 Phase 0 未通过项都不能通过修改验收阈值、移除测试或启用规则动作混合来规避。
